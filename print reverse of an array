using System;
class HelloWorld {
  static void Main() {
    int[] arr = new int[5];
    for(int i=0;i<arr.Length;i++){
        Console.WriteLine($"enter {i+1} element");
        arr[i]=int.Parse(Console.ReadLine());
    }
    for(int i=arr.Length;i>0;i--){Console.Write(arr[i-1]+"  ");}
    
  }
}
